# MicroSim TODO Generation — Status Log

**Date:** 2026-06-05
**Skill:** `microsim-utils`
**Utility:** `scripts/create-microsim-todo-json-files.py`
**Command:**

```bash
python3 ~/.claude/skills/microsim-utils/scripts/create-microsim-todo-json-files.py \
  --project-dir /Users/dan/Documents/ws/modeling-healthcare-data
```

## Result Summary

| Metric | Value |
|--------|------:|
| Chapters scanned | 12 |
| Diagram specs found | 91 |
| Reported as already implemented (have `main.html`) | 0 |
| TODO JSON files written | 91 |
| Output directory | `docs/sims/TODO/` |

Each TODO file (`docs/sims/TODO/{sim-id}.json`) contains: `sim_id`,
`diagram_name`, `chapter_number`, `chapter_title`, `chapter_dir`, `library`,
`bloom_level`, `bloom_verb`, `learning_objective`,
`completion_status: "specified"`, `extracted_date`, and the full
`specification` text.

## TODO Files by Chapter

| Ch | Title | TODO specs |
|----|-------|-----------:|
| 01 | Graph Theory & Database Foundations | 6 |
| 02 | Introduction to Healthcare Systems | 7 |
| 03 | Graph Query Languages | 9 |
| 04 | Patient-Centric Data Modeling | 9 |
| 05 | Provider Operations & Networks | 7 |
| 06 | Payer Perspective & Insurance | 6 |
| 07 | Healthcare Financial Analytics | 7 |
| 08 | Fraud Detection & Compliance | 6 |
| 09 | Graph Algorithms & Analytics | 7 |
| 10 | AI & Machine Learning Integration | 9 |
| 11 | Security, Privacy & Governance | 9 |
| 12 | Capstone & Real-World Applications | 9 |
| | **Total** | **91** |

## TODO Files by Library

| Library | Count |
|---------|------:|
| p5.js | 40 |
| vis-network | 20 |
| Chart.js | 16 |
| Mermaid | 12 |
| vis-timeline | 2 |
| Venn.js | 1 |

All 91 TODO files have `completion_status: "specified"`.

## Known Issues / Caveats

### 1. Four specs are already built under mismatched directory names

The extractor reported **0 already-implemented**, but four chapters embed
working `<iframe>` sims whose built directory name does not match the spec's
`sim-id`. As a result, these four were written as TODO files even though a
runnable sim already exists:

| Built directory (has `main.html`) | TODO file created |
|-----------------------------------|-------------------|
| `ai-ml-taxonomy` | `ai-ml-taxonomy-healthcare.json` |
| `encounter-workflow` | `clinical-encounter-workflow-diagram.json` |
| `healthcare-graph-fundamentals` | `basic-healthcare-graph-model-diagram.json` |
| `query-performance-comparison` | `query-performance-comparison-chart-rdbms-vs-graph-database.json` |

**Effective unimplemented count: ~87** (91 minus these 4 duplicates).

**Recommended fix:** align each of these four spec `sim-id` fields to the built
directory name (their iframes already use the built name), then re-run the
extractor so they are correctly skipped — or delete the four redundant TODO
files.

### 2. `bloom_level` field is null

The extractor populates `bloom_level` / `bloom_verb` only when a spec has a
dedicated `Bloom:` line. This book's specs encode the Bloom level inline in the
`learning_objective` (e.g., "Analyze (L4 — differentiate, compare) ...") and in
the full `specification`, so the dedicated `bloom_level` field is `null` in all
91 files. The information is still present in the objective text.

## Next Steps

1. **Reconcile** the four sim-id/directory mismatches (see Caveat 1).
2. **Scaffold** stub directories from the TODO JSON:
   `python3 ~/.claude/skills/microsim-utils/scripts/scaffold-microsims-from-todo.py --project-dir .`
3. **Build** individual sims with `microsim-generator` (p5.js / Chart.js) or
   `hc-graph-generator` (vis-network graph models) against a chosen `sim-id`.

---

*Generated by the `microsim-utils` skill. See
[microsim-coverage-report.md](../docs/learning-graph/microsim-coverage-report.md)
for the full specification inventory and coverage analysis.*

---

# MicroSim Generation Progress (microsim-generator skill)

**Started:** 2026-06-05 · **Mode:** full pipeline per sim (implement → measure/fix
iframe height → screenshot → Claude Vision layout review → enrich index.md →
validate → commit). **Order:** by library, simplest first. Each sim is committed
individually to `main`.

## Scope reconciliation

- 91 TODO specs total. **4 already satisfied** by built + chapter-embedded sims
  under different directory names — excluded from generation:
  `ai-ml-taxonomy-healthcare` (→ `ai-ml-taxonomy`),
  `basic-healthcare-graph-model-diagram` (→ `healthcare-graph-fundamentals`),
  `clinical-encounter-workflow-diagram` (→ `encounter-workflow`),
  `query-performance-comparison-chart-rdbms-graph` (→ `query-performance-comparison`).
- **87 specs to generate.** All 87 scaffolded (main.html/index.md/metadata.json).
- Library counts to build: vis-timeline 2, Mermaid 11, Chart.js 15, p5.js 40, vis-network 19.
  (The single Venn.js spec was the already-built `ai-ml-taxonomy`.)

## Completed (committed to main) — 32 of 87

**vis-timeline: 2/2** · **Mermaid: 11/11** · **Chart.js: 15/15** · **p5.js: 4/40**

p5.js done so far: bayesian-diagnostic-reasoning, value-based-payment-shared-savings,
medical-code-classifier, anomaly-score-threshold-explorer (all 98/A — full pipeline).

Each sim was built through the full pipeline (implement .js → measure/fix iframe
height → screenshot → Claude Vision layout review → enrich index.md → validate
100/A → individual git commit). `mkdocs.yml` nav regenerated (96 MicroSim entries).

- Chart.js sims: care-pathway-comparison-chart, comparison-graph-db-llm-integrated-system,
  data-quality-dashboard-chart (2x2), denial-reasons-analysis-chart, migration-strategy-comparison-chart,
  patient-outcome-metrics-dashboard-chart, payer-mix-contract-negotiation (interactive calculator),
  per-person-healthcare-cost-comparison-chart, predictive-model-performance-traditional-graph-based,
  project-complexity-comparison-table, provider-capacity-utilization-dashboard-chart,
  provider-compensation-comparison-chart, provider-network-centrality-analysis-dashboard (interactive),
  provider-network-fraud-detection-dashboard (2x2), query-performance-impact-indexing.
  Pattern: titled `<canvas>` in a fixed-height `.cc-chartbox`, `responsive:true` +
  `maintainAspectRatio:false`, tooltips always on, log scales / dual axes / radar as
  the data needs. Heights are stable (~470-560 single, ~750-860 for 2x2 dashboards).

## Remaining — 55

- **p5.js (36 left):** scaffold loads `p5@1.11.10` + `<sim-id>.js`. **Working pattern**
  (from the 4 done): top-level vars `containerWidth/canvasWidth/drawHeight/controlHeight/
  canvasHeight`; `setup()` calls `updateCanvasSize()` then `createCanvas(...).parent(
  document.querySelector('main'))`; draw region = `aliceblue` rect (0,0,w,drawHeight),
  control region = white rect below; centered title; built-in DOM controls positioned at
  `drawHeight + offset` and re-laid-out in `windowResized()`; `describe(..., LABEL)` at end
  of setup (note: LABEL renders a visible caption below the canvas — the measurer counts it,
  so `/tmp/cal.sh` returns canvasHeight + ~56). GOTCHA: `text(str, x, y, w)` treats x as the
  box LEFT (not center) — pass the left edge + width, not `canvasWidth/2`, or text clips right.
  Many are Apply/Evaluate interactives (calculators, classifiers, explorers); a handful are
  static "infographics" (cypher-query-components, data-integration-flow, medical-coding-systems,
  healthcare-ecosystem-stakeholder, etc.) that are labeled diagrams.
- **vis-network (19):** not yet started. Read `references/vis-network-guide.md`. Graph
  data-model / network visualizations; use the measure→set-CANVAS_HEIGHT→capture loop
  (`/tmp/cal.sh`) like Mermaid since rendered height varies.

### Pending follow-up (NOT done, touches user's uncommitted chapter edits)

Chapter `index.md` files in `docs/chapters/*/` have pre-existing uncommitted edits
(the spec blocks). The new sims are built and in the nav but their `<iframe>` embeds
have **not** been inserted into the chapter pages. To wire them in, run (and review,
since it co-mingles with the user's WIP):
`python3 $UTILS/add-iframes-to-chapter.py --all --project-dir . --fix-heights --fix-paths`

## Technical notes / reusable tooling (for resumption)

- **Mermaid pattern:** scaffold main.html loads `mermaid@10` + `<sim-id>.js`. The
  `.js` injects CSS, builds a 64% diagram + 36% hover info panel inside `<main>`,
  defines the flowchart + `nodeInfo`, calls `mermaid.initialize({startOnLoad:false,…})`
  then `mermaid.run({querySelector:'.mermaid'}).then(setupHover)`. Hover keys = node
  IDs (mermaid emits `flowchart-<ID>-n`).
- **GOTCHA:** never name a Mermaid `classDef` `graph` (or other reserved keywords) —
  it triggers "Syntax error in text". Use e.g. `gquery`. (Detected via height
  measurer returning a tiny ~207 height = render failure.)
- **Height calibration:** `/tmp/measure_height.py <sim-dir>` (Playwright, 800px wide)
  prints exact rendered px height; `/tmp/cal.sh <sim-id>` measures → writes
  `// CANVAS_HEIGHT:` → runs `fix-iframe-heights.py` → captures the screenshot.
- **index.md enrichment:** replace the scaffold block (the `[Edit in the p5.js Editor]`
  link + `## About`/`## How to Use` TODOs) with real prose; validator scores section
  *presence*, so all completed sims validate at 100/A.
